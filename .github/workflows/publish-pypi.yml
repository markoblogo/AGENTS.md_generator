name: Publish PyPI

on:
  release:
    types: [published]
  workflow_dispatch:
    inputs:
      tag:
        description: "Existing release tag to publish (for retry/manual publish)"
        required: true
        type: string

permissions:
  contents: read
  id-token: write

concurrency:
  group: pypi-publish-${{ github.event.release.tag_name || inputs.tag }}
  cancel-in-progress: false

jobs:
  publish-pypi:
    if: github.event_name != 'release' || github.event.release.prerelease == false
    runs-on: ubuntu-latest
    environment: pypi
    steps:
      - name: Resolve tag
        id: ref
        env:
          RELEASE_TAG: ${{ github.event.release.tag_name }}
          INPUT_TAG: ${{ inputs.tag }}
        run: |
          tag="${RELEASE_TAG:-$INPUT_TAG}"
          if [ -z "$tag" ]; then
            echo "Missing release tag." >&2
            exit 1
          fi
          echo "tag=$tag" >> "$GITHUB_OUTPUT"

      - uses: actions/checkout@v7
        with:
          ref: ${{ steps.ref.outputs.tag }}

      - uses: actions/setup-python@v7
        with:
          python-version: "3.11"

      - name: Verify tag matches package version
        run: |
          python - <<'PY'
          import pathlib
          import tomllib

          tag = "${{ steps.ref.outputs.tag }}"
          version = tomllib.loads(pathlib.Path("pyproject.toml").read_text())["project"]["version"]
          expected = f"v{version}"
          if tag != expected:
              raise SystemExit(f"Tag/version mismatch: tag={tag}, pyproject={expected}")
          print(f"Publishing {expected}")
          PY

      - name: Validate release source
        run: |
          python -m pip install -e ".[dev]"
          ruff check .
          ruff format --check .
          mypy src
          pytest -q
          agentsgen check . --all --ci

      - name: Download reviewed release artifacts
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          mkdir -p dist
          gh release download "${{ steps.ref.outputs.tag }}" --dir dist --pattern '*.whl' --pattern '*.tar.gz'
          test "$(find dist -maxdepth 1 -name '*.whl' | wc -l | tr -d ' ')" = "1"
          test "$(find dist -maxdepth 1 -name '*.tar.gz' | wc -l | tr -d ' ')" = "1"
          python -m pip install --upgrade twine
          twine check dist/*

      - name: Verify installed wheel
        run: |
          python -m venv /tmp/agentsgen-wheel
          /tmp/agentsgen-wheel/bin/pip install dist/*.whl
          python scripts/release_smoke.py --exe /tmp/agentsgen-wheel/bin/agentsgen

      - name: Publish to PyPI
        uses: pypa/gh-action-pypi-publish@release/v1
